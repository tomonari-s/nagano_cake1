<p></p>

<div class="container">
  <div class ="col-md-3">
    <h3 class ="bg-light", align="center">注文情報入力</h3>
  </div>
<h2>マイページ</h2>
<p>登録情報</p><%= link_to "編集する", edit_customer_path(@customer.id), class:"btn btn-sm btn-success" %>

  <table class="table table-hover table-inverse">
    <thead>
      <tr>
        <th>氏名</th>
        <td><%= @customer.last_name %> <%= @customer.first_name %></td>
      </th>  
      <tr>
        <th>カナ</th>
        <td><%= @customer.last_name_kana %> <%= @customer.first_name_kana %></td>
      </th>
      <tr>
        <th>郵便番号</th>
        <td><%= @customer.postal_code %></td>
      </th>
      <tr>
        <th>住所</th>
        <td><%= @customer.address %></td>
      </th>
      <tr>
        <th>電話番号</th>
        <td><%= @customer.telephone_number %></td>
      </th>
      <tr>
        <th>メールアドレス</th>
        <td><%= @customer.email %></td>
      </th>
  
  
  <h2>配送先</h2><%= link_to "一覧を見る", deliveries_path, class:"btn btn-sm btn-success" %>
  <h2>注文履歴</h2><%= link_to "一覧を見る", orders_path, class:"btn btn-sm btn-success" %>
</div>